// import * as types from './../types'

const initialState = {
    shows: []
}

export default function(state = initialState, action) {
    switch(action.type) {

        default: 
            return state
    }
}